import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true, // React Strict Mode 활성화
  swcMinify: true, // SWC를 사용하여 빌드 최적화
};

export default nextConfig;
